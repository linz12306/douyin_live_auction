# 2026-06-08

## merchant-ui-optimization

- 分支：`codex/merchant-ui-optimization`
- OpenSpec change：`openspec/changes/merchant-ui-optimization/`
- 用户确认的视觉方向：深色专业 SaaS 直播电商运营控制台，墨黑/石墨底色，青绿色表示进行中/成交，琥珀表示待开拍/待处理，玫红表示取消/风险，蓝色表示支付/信息态；商品管理采用横向直播商品控盘行，不走普通表格后台。
- 已完成商家 PC 端页面视觉统一：`/merchant/dashboard`、`/merchant/products`、`/merchant/products/:id`、`/merchant/products/:id/edit`、`/merchant/products/new`、`/merchant/orders`、`/merchant/orders/:id`、`/merchant/auctions/:id/monitor`。
- 新增共享前端组件：`frontend/src/components/merchant/MerchantConsole.tsx`、`MerchantPrimitives.tsx`、`merchantStatus.ts`，仅服务商家端展示层。
- 保持后端 API、数据库、竞拍状态机、WebSocket 语义、订单/钱包规则不变；后端文件未修改。
- 商品详情/表单补充非法 route ID 防护，避免 bad route 触发 `/NaN` API；图片和直播素材上传入口改为键盘可操作控件，规则分段按钮暴露 `aria-pressed`。
- 验证：
  - `cd frontend && npm run test` passed with 16 files / 82 tests.
  - `cd frontend && npm run build` passed.
  - `git diff --check` passed.
  - `npx -y @fission-ai/openspec@latest validate merchant-ui-optimization --strict --no-interactive` passed.
  - Browser smoke started Vite at `http://127.0.0.1:3000/`; merchant routes correctly redirected to `/login` without console errors. Auth/backend session was unavailable, so authenticated merchant screenshots were not captured.
- 下一步可继续精修：接入真实登录态后的桌面/窄屏截图验收、微交互动效、横向商品行列宽在真实长标题/多状态数据下的再调校。
