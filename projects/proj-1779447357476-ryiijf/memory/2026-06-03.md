# 2026-06-03 Memory

## h5-discovery-live-feed

- Branch: `codex/frontend-experience-integration`.
- Change: `openspec/changes/h5-discovery-live-feed/`.
- Implemented `/app/auctions` as an H5 `发现竞拍` discovery live-feed entrance.
- Files changed:
  - `frontend/src/pages/app/AuctionLobby.tsx`
  - `frontend/src/pages/app/AuctionLobby.test.tsx`
  - `docs/superpowers/plans/2026-06-02-h5-discovery-live-feed.md`
  - `openspec/changes/h5-discovery-live-feed/tasks.md`
  - `projects/proj-1779447357476-ryiijf/memory/long-term.md`
- Implementation details:
  - Kept `listAuctionLobby()` as the only data source.
  - Added local search over currently loaded item titles.
  - Added channel chips: `推荐`, `正在竞拍`, `快结束`, `待开拍`.
  - Added active-first hero card, secondary feed cards, image placeholders, discovery header, order/profile links, retry button, API-empty state, and local filtered-empty state.
  - Added `aria-pressed` on filter chips and distinct auction-entry accessible names while preserving visible `进入直播` text.
- Verification:
  - `cd frontend && npm run test -- AuctionLobby` passed with 7 tests.
  - `cd frontend && npm run build` passed.
  - `npx -y @fission-ai/openspec@latest validate h5-discovery-live-feed --strict --no-interactive` passed.
  - `git diff --check` passed.
  - Screenshot smoke captured:
    - `/tmp/h5-discovery-live-feed-mobile.png`
    - `/tmp/h5-discovery-live-feed-filtered-empty.png`
- Review:
  - Subagent spec compliance review passed.
  - Subagent code-quality review found accessibility issues; fixes were applied and re-review passed.

## merchant-live-media

- Branch: `codex/frontend-experience-integration`.
- Change: `openspec/changes/merchant-live-media/`.
- Implemented one merchant-managed live room media asset per product:
  - Backend migration/model/repository support for `product_live_media`.
  - Product detail and realtime snapshot payloads now expose optional `live_media`.
  - Merchant upload/replace/delete API uses `/api/v1/products/:id/live-media`, stores files under `/static/live-media`, validates ownership/status/type/content/size, and cleans replaced/deleted files.
  - `ProductForm` now has a `直播间素材` block with preview, local validation, create-flow pending upload, edit-flow direct upload/delete, and readonly behavior outside draft.
  - `LiveAuctionRoom` renders uploaded image/video media as the stage background before falling back to the current staged scene.
  - Demo seed uploads a small PNG through the live-media API and keeps lobby/order summary images sourced from `product_images`.
- Preview:
  - Backend running on `http://127.0.0.1:8080`.
  - Frontend running on `http://127.0.0.1:13000`.
  - Seed run `mpxxsy9z` created auction `1802`, product `1985`, route `/app/auctions/1802`.
  - Verified `demo_merchant / test123` reaches `/merchant/products` and sees `直播间素材` on `/merchant/products/new`.
  - Verified `demo_buyer_a / test123` sees `Demo Auction mpxxsy9z` in `/app/auctions` and live media in `/app/auctions/1802`.
  - Screenshot saved at `/tmp/douyin-live-media-preview-1802.png`.
- Verification:
  - `cd frontend && npm run test -- AuctionLobby` passed.
  - `cd frontend && npm run test -- ProductForm LiveAuctionRoom` passed.
  - `cd backend && go test ./tests/integration -run 'TestProductLiveMedia|TestDeleteDraftProductRemovesLiveMediaFile|TestProductDetailIncludesLiveMedia|TestUserListsActiveAuctionLobbyRows|TestOrderAPIUsesProductImageWhenLiveMediaExists' -count=1` passed.
  - `cd backend && go test ./internal/handler ./internal/realtime -count=1` passed.
  - `node --test scripts/demo-seed.test.mjs` passed.
  - `cd frontend && npm run build` passed.
  - `npx -y @fission-ai/openspec@latest validate merchant-live-media --strict --no-interactive` passed.
  - `git diff --check` passed.
- Commit/push intentionally not done yet; user asked that future workflow gates ask for review before commit/push.
