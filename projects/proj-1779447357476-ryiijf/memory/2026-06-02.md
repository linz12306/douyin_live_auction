# 2026-06-02 Memory

## auction-atmosphere

- Branch: `codex/auction-atmosphere-h5`.
- Change: `openspec/changes/auction-atmosphere/`.
- Scope delivered: buyer H5 `/app/auctions/:id` live-room atmosphere only.
- Implemented full-screen mobile-first live commerce room shell, host bar, live/status/rule badges, product/scene background, comments/system messages, right-side atmosphere controls, bottom commerce controls, persistent auction floating card, half-screen bid sheet, half-screen product shelf shell, and in-room terminal result modal.
- Preserved WebSocket as realtime truth source. REST bid command still only drives local submitting/error state and does not update visible current price, ranking, countdown, extension count, leading/outbid state, or terminal state.
- Product shelf shell keeps only the current auction item realtime-backed. Other rows are labeled as `演示货架`.
- Result modal uses existing buyer order entry route `/app/orders`; confirmation and simulated payment remain in order pages.
- E2E selectors were updated to open the new bid sheet before placing bids in realtime/demo tests.

## merchant-analytics

- Branch: `codex/merchant-analytics-dashboard`.
- Change: `openspec/changes/merchant-analytics/`.
- Scope delivered: merchant PC dashboard analytics for `/merchant/dashboard`.
- Extended existing `GET /api/v1/merchant/dashboard` response with read-only `analytics`:
  - `transaction_trend`: last 7 days paid amount and paid order count.
  - `bid_distribution`: stable bid amount buckets `0-99`, `100-499`, `500-999`, `1000-4999`, `5000+`.
  - `user_activity`: last 7 days active bidder count and bid count.
- Backend aggregates remain scoped to current merchant. Orders filter by `orders.merchant_id`; bid analytics join merchant-owned auctions.
- Frontend now renders compact CSS-based charts for transaction trend, bid distribution, and user activity. Existing summary metrics, status buckets, active auctions, recent orders, loading/error, and navigation remain.
- Dashboard background was adjusted to a quieter PC operations style instead of the previous purple marketing-like gradient.

## Integration

- Integration branch: `codex/frontend-experience-integration`.
- Merged `codex/auction-atmosphere-h5`.
- Merged `codex/merchant-analytics-dashboard`.
- Merge conflicts were limited to project memory files; code and OpenSpec files did not conflict.

## Verification

- `cd frontend && npm run test -- LiveAuctionRoom` passed for `auction-atmosphere`.
- `cd frontend && npm run test` passed with 62 tests for `auction-atmosphere`.
- `cd frontend && npm run build` passed for `auction-atmosphere`.
- `npx playwright test tests/e2e/realtime-live-room.spec.ts tests/e2e/demo-readiness.spec.ts --list` passed for `auction-atmosphere`.
- Browser screenshot/layout check passed with mocked auth and WebSocket snapshot for 390x844 and 1200x900. Screenshots: `/tmp/auction-atmosphere-mobile.png`, `/tmp/auction-atmosphere-desktop.png`.
- `npx -y @fission-ai/openspec@latest validate auction-atmosphere --strict --no-interactive` passed.
- `REDIS_ADDR=127.0.0.1:16379 go test ./tests/integration -run TestMerchantDashboard -count=1` passed for `merchant-analytics`.
- `cd frontend && npm run test -- Dashboard` passed for `merchant-analytics`.
- `cd frontend && npm run test` passed with 61 tests for `merchant-analytics`.
- `cd frontend && npm run build` passed for `merchant-analytics`.
- Browser smoke check with mocked merchant auth/dashboard API passed and saved `/tmp/merchant-analytics-dashboard.png`.
- `npx -y @fission-ai/openspec@latest validate merchant-analytics --strict --no-interactive` passed.
- `git diff --check` passed for both individual packages.

## Notes

- Full two-buyer Playwright E2E was not run in the `auction-atmosphere` slice because backend/MySQL/Redis services were not started for that turn. The updated tests parse and list successfully.
- No database migration was needed for `merchant-analytics`.
- No auction, wallet, order, settlement, payment, cancellation, WebSocket, buyer H5, demo material, or observability semantics were changed by `merchant-analytics`.
- The focused backend integration test used Redis `127.0.0.1:16379` because `127.0.0.1:16380` was not running in this local environment.
- Keep `frontend-experience-roadmap` active. Do not archive it yet.
