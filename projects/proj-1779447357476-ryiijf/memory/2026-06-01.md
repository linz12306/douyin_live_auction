# 2026-06-01 Memory

## perf-observability

- Branch/worktree: `/Users/vivix/Documents/Codex/douyin_live_auction_worktrees/perf-observability`, branch `codex/perf-observability`.
- Full workflow used because the change touches observability, auction-engine instrumentation, WebSocket runtime stats, and judge-facing performance evidence.
- Created exploration: `docs/superpowers/specs/2026-06-01-perf-observability-exploration.md`.
- Created execution plan: `docs/superpowers/plans/2026-06-01-perf-observability.md`.
- Archived OpenSpec change: `openspec/changes/archive/2026-06-01-perf-observability/`.
- Persistent spec updated: `openspec/specs/observability-health/spec.md`.

### Implemented

- Added process-local `AuctionMetrics` with atomic counters for:
  - bid request total
  - bid success total
  - bid failure total
  - bid success rate
  - average bid latency in milliseconds
  - lock-busy total
- Instrumented `AuctionService.PlaceBid` after user-role validation, without changing bid validation, wallet mutation, transaction, settlement, audit-log, or realtime event semantics.
- Counted Redis bid lock contention when `SETNX` reports the auction lock is already held.
- Extended `/healthz` `components.auction_engine` with:
  - `bid_requests_total`
  - `bid_success_total`
  - `bid_failure_total`
  - `bid_success_rate`
  - `bid_avg_latency_ms`
  - `bid_lock_busy_total`
  - `ws_connections_current`
- Added `scripts/load-auction.mjs` for local concurrent bid load and optional WebSocket room connections.
- Added `docs/performance-report.md` with setup commands, metric definitions, a fill-in results table, and a local single-process scope narrative.

### Verification Notes

- Focused RED/GREEN tests covered the metrics collector, health response mapping, and service instrumentation helpers.
- The exact requested backend command needs Redis at the repository default `127.0.0.1:16380`; on this machine Redis is running at `127.0.0.1:16379`, so a temporary local TCP proxy `16380 -> 16379` was used only while running that command, then closed.
- Final verification passed:
  - `cd backend && /Users/vivix/.local/go/bin/go test ./...`
  - `node scripts/load-auction.mjs --help`
  - `npx -y @fission-ai/openspec@latest validate perf-observability --strict --no-interactive`
  - `npx -y @fission-ai/openspec@latest validate --specs --strict --no-interactive`
  - `git diff --check`
