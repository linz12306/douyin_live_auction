# 2026-05-30 Memory

## merchant-auction-monitor

- Implemented and verified the `merchant-auction-monitor` OpenSpec change in worktree `D:\pythoncode\douyin-live\.worktrees\merchant-auction-monitor` on branch `codex/merchant-auction-monitor`.
- Added merchant navigation to `/merchant/auctions/:id/monitor` from product list/detail. Merchant list rows now expose optional `auction_id` for published products.
- Added a merchant realtime monitor page backed by the existing `/ws/auctions/:id` live room store. It shows current price, countdown, terminal state, rankings, and a bid-event feed, and intentionally has no bid controls.
- Kept cancellation on the existing REST path and surfaces the rule text `最后出价后 30 秒内不可取消` before allowing a reasoned cancellation.
- Stabilized shared MySQL tests by serializing DB-using Go test packages with an advisory lock and by using per-test auction usernames. Also aligned direct SQL timestamp setup with Go time and persisted service-owned `created_at`/`updated_at` for bids/orders.
- Verification passed:
  - `openspec validate merchant-auction-monitor --strict --no-interactive`
  - `cd backend && $env:REDIS_ADDR='127.0.0.1:16380'; go test ./...`
  - `cd frontend && npm run test`
  - `cd frontend && npm run build`
  - `git diff --check`
- Commit/push state: verified slice prepared for `feat(merchant): add auction monitor` on branch `codex/merchant-auction-monitor`.
- Next step: archive the OpenSpec change only after acceptance.

## Merchant Dashboard

- Implemented OpenSpec change `merchant-dashboard`.
- Added backend `GET /api/v1/merchant/dashboard` for merchant role users only.
- Dashboard response includes product status counts, order status counts, paid transaction summary, active auction summaries, and recent orders scoped to the current merchant.
- Transaction metrics intentionally count only `paid` orders: `total_paid_amount`, `paid_order_count`, and `average_paid_price`.
- Added frontend `/merchant/dashboard` with metric cards, product/order status sections, active auctions, recent orders, loading/error states, and links back to product/order management.
- Added dashboard entry links from merchant product list, merchant order list, and profile.

## Verification

- `openspec validate merchant-dashboard --strict --no-interactive` passed.
- `openspec validate --specs --strict --no-interactive` passed.
- `cd backend && go test ./...` passed with `REDIS_ADDR=127.0.0.1:16380` and `DB_DSN=root:auction123@tcp(127.0.0.1:3307)/auction_db_merchant_dashboard?parseTime=true&loc=UTC&charset=utf8mb4`.
- `cd frontend && npm run test` passed.
- `cd frontend && npm run build` passed.
- `git diff --check` passed with line-ending warnings only.
- Browser smoke test passed at `http://127.0.0.1:5177/merchant/dashboard` using worktree backend on `http://127.0.0.1:18080`.

## Environment Notes

- This worktree uses Redis `127.0.0.1:16380` for backend and integration tests.
- Local default `auction_db` was being affected by an existing backend/server process and historical test data, producing foreign-key and missing-image flakes during full integration verification.
- To isolate verification, created local DB `auction_db_merchant_dashboard` from migrations `001` through `007`.
- Backend tests require UTC location in the MySQL DSN for reliable comparisons because MySQL `NOW()` and Go local time otherwise differ in this environment.

## fix-self-outbid-notification

- Opened branch `codex/fix-self-outbid-notification` from synced `master`.
- Root cause: `AuctionService.PlaceBid` emitted `EventBidOutbid` whenever any previous active bid existed, including when `previousBid.UserID == userID`.
- Locked OpenSpec change `fix-self-outbid-notification` to clarify that private `outbid` messages are only for bids replaced by a different user.
- Added backend integration test `TestSameUserRebidDoesNotPublishOutbidEvent`; it failed before the fix with an unexpected `bid.outbid` event and passed after the fix.
- Implemented the minimal guard `previousBid != nil && previousBid.UserID != userID` for `EventBidOutbid` creation.
- Verification passed:
  - `openspec validate fix-self-outbid-notification --strict`
  - `cd backend && $env:GOPROXY='https://goproxy.cn,direct'; go test ./tests/integration -run "TestSameUserRebidDoesNotPublishOutbidEvent|TestOutbidPublishesRealtimeEvent" -count=1`
  - `cd backend && $env:GOPROXY='https://goproxy.cn,direct'; go test ./internal/realtime ./internal/service ./tests/integration -count=1`
  - `git diff --check` passed with Windows line-ending warnings only.
- Local backend restarted on `127.0.0.1:8080`; frontend remains on `127.0.0.1:3000`.
- Next step: user should retest the single-bidder self-rebid flow in the live room; archive the OpenSpec change after acceptance.
